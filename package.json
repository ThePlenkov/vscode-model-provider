{
  "name": "vscode-model-provider-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "libs/*",
    "packages/*"
  ],
  "scripts": {
    "nx": "nx",
    "build": "npm run build -ws --if-present",
    "build:extension": "npm run build -w vscode-model-provider",
    "test": "npm run test -ws --if-present",
    "typecheck": "npm run typecheck -ws --if-present",
    "lint": "npm run lint -ws --if-present",
    "graph": "nx graph"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@nx/js": "23.0.1",
    "@types/node": "^20.0.0",
    "eslint": "^9.0.0",
    "nx": "23.0.1",
    "tsdown": "^0.22.3",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.0.0"
  }
}